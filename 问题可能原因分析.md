# 问题可能原因分析

读完 `@/tmp/zfj/apps/face-swap-video/server/api/server.py` 与 `@/tmp/zfj/apps/facefusion-fc-gpu-acr/Dockerfile`、[docker/start_server.sh](cci:7://file:///tmp/zfj/apps/facefusion-fc-gpu-acr/docker/start_server.sh:0:0-0:0) 后，结合文档里 "进度卡 85%、超时、0 字节输出" 的现象，下面分**症状层 → 根因候选 → 可能的次生坑** 三类列出，按可能性高低排序。

## 1. 症状层：先纠正一个误导

**`85%` 不是真实进度，是合成上限。** 见 `_track_facefusion_stage`（约 350 行）：

```python
progress = min(0.85, 0.25 + (elapsed - 6) / 90 * 0.55)
_update_job_stage(job_id, "swapping_frame", progress)
```

- 进度只是按"经过了多少秒"线性插值，从 6s 起到 96s 累加到 0.85 后**永远不动**，与 FaceFusion CLI 真实进度无关；
- FaceFusion stdout/stderr 全部重定向到 [/tmp/facefusion_*_*.log](cci:1://file:///Users/weisiwu_clawbot_mac/Desktop/%E8%87%B4%E5%AF%8C%E7%BB%8F/apps/face-swap-video/app/lib/core/services/app_logger.dart:89:2-117:3)，没有任何 stdout 解析回填进度。

所以 "卡在 85%" 仅意味着 **FaceFusion 子进程已运行 ≥ 96s 还没退出**。后续根因不要把 85% 当真实瓶颈。

## 2. 根因候选（按可能性排序）

### A. **In-memory `JOBS` + FC 多实例 / 实例冻结** ⭐⭐⭐ 最可能

```python
JOBS: dict[str, dict] = {}
JOB_PROCESSES: dict[str, subprocess.Popen] = {}
```

- 状态完全存在进程内；
- FC custom container 触发扩容后 job 提交到 A 实例，下一次 poll 路由到 B 实例直接 `404 not found`；
- 即使没扩容，**FC 在长时间无 HTTP 流量时会冻结实例**：`FF_VIDEO_JOB_MODE=background` 把 FaceFusion 跑在 `ThreadPoolExecutor` 的后台线程，而 FC 只把"前台请求时长"算作活跃；后台线程在实例被冻结/回收瞬间被 SIGTERM，App 端就会看到"超时、0 字节、文件不存在"的组合。
- App 端轮询会"喂活" 该实例，但只要单次 poll 间隔大于 FC 空闲冻结阈值，或某次 poll 落到另一个实例，链路即断。

### B. **FaceFusion 视频流水线对 FC 临时盘的依赖** ⭐⭐⭐

FaceFusion 原生视频流水线流程是：

```
ffmpeg 抽帧 → <FF_DIR>/.assets/temp/<basename>/*.jpg
    → 逐帧推理生成 *.swap.jpg
    → ffmpeg 合成最终 mp4
```

- 容器中 `WORKDIR=/opt/facefusion/server`，`.assets/temp` 写在镜像层下；很多 FC 镜像层是只读 overlay，**FaceFusion 默认输出帧的目录写不动**，会反复重试或卡在 ffmpeg `mkdir`。
- 即便能写，FC GPU 实例 `/` 容量通常很有限（几 GB）。preview profile 12fps×360 宽，30s 视频 ≈ 360 帧，单帧 ~150KB，本身不大；但 **inswapper** 的 detector/landmarker/face_recognizer 等若按需下载就会把空间和时间双吃光。
- 现在调用没传 `--temp-path`、`--keep-temp`、`--video-memory-strategy`，全部走默认。

### C. **FaceFusion 启动时的"按需模型下载"在 FC 上挂死** ⭐⭐⭐

`_run_facefusion` 实际下发的参数仅：

```
--processors face_swapper
--face-swapper-model inswapper_128
--execution-providers cuda
--execution-thread-count 1
--output-video-preset/quality/fps
--log-level debug
```

没有显式声明 `--face-detector-model`、`--face-landmarker-model`、`--face-recognizer-model`。这些走默认值（多为 `retinaface_resnet50` / `2dfan4` / `arcface_inswapper` 之类），如果**镜像里 `.assets/models/` 没有这些 onnx**，FaceFusion 第一次 video 任务会触发从 HuggingFace / GitHub releases 拉模型。

- 阿里云 FC 出网到 `huggingface.co`、`github.com` **经常 DNS 解析慢或被 reset**；
- 拉模型用的是 `requests`，默认是阻塞调用，没有超时上限；
- 这恰好解释"图片换脸能成（之前已经把所有模型下完缓存上）但 video job 第一次跑就长时间卡住"。

⛳️ 验证方法：进容器看 `~/.config/facefusion` 或 `<FF_DIR>/.assets/models/` 是否齐备；或在 stderr 里 grep `Downloading` / `urllib3`。

### D. **`onnxruntime-gpu 1.23.2` 与 FC GPU 主机驱动版本错配** ⭐⭐

- 镜像基线 `nvidia/cuda:12.6.3-cudnn-runtime-ubuntu24.04`，`onnxruntime-gpu==1.23.2` 期望 CUDA 12.6/12.9；
- FC GPU 实例宿主机驱动若停留在 12.4/12.2，**`/api/health` 中 `ort.get_available_providers()` 仍可能返回 `['CUDAExecutionProvider', ...]`**（这只是编译期可用列表，不代表能创建 session）；
- 真正在 worker 里 `InferenceSession(..., providers=['CUDAExecutionProvider'])` 时静默回退 CPU，速度从 ~30 fps 掉到 ~0.5 fps，preview 30s 视频要跑十几分钟，**直接撞 `FF_FACEFUSION_TIMEOUT_SECONDS`（默认 900s，但你的部署里看起来是 600）**。

⛳️ 验证方法：在 `_run_facefusion` 调用前 print 一次 `ort.get_device()` / 让 FaceFusion `--log-level debug` 打印 provider，或在容器里跑：

```bash
python -c "import onnxruntime as ort; \
  s=ort.InferenceSession('<some>.onnx', providers=['CUDAExecutionProvider']); \
  print(s.get_providers())"
```

### E. **极端单线程 + 不必要的 BLAS 串行化** ⭐⭐

```dockerfile
ENV FF_EXECUTION_THREAD_COUNT=1
    OMP_NUM_THREADS=1
    OPENBLAS_NUM_THREADS=1
    MKL_NUM_THREADS=1
    VIDEO_MAX_WORKERS=1
```

GPU 推理本身不受 OMP_NUM_THREADS 影响，但 FaceFusion 视频管线里 **预/后处理 (resize、color convert、写 jpg) 全在 CPU**。把 OMP 全锁 1 之后，frame 处理串行，常常成为真正瓶颈，**不会失败但会拖到超时**。
preview profile 12fps + 360 width 的"15 秒以内"片子，180 帧 × 单线程 CPU pre-process ≈ 几十秒到一两分钟，叠加 D 项就足以踩到超时。

### F. **`raw_out_path.exists()` 不能识别"半截 mp4"** ⭐⭐

```python
if exit_code != 0 or not raw_out_path.exists():
    raise RuntimeError(...)
```

- 当 FaceFusion 被 SIGTERM/超时 kill 时（见 `_terminate_process` 用 `killpg`），FaceFusion 内部 ffmpeg 已经创建了 `output.mp4` 但 **没写 `moov`**；
- 文件确实"存在"，`exit_code` 也可能是 124 → 这条 if 会按 124≠0 抛 `RuntimeError`，但抛之前 `output_bytes` 已被记录，App 拿到的就是 0 字节或几 KB 的破文件。
- 这与文档里 "输出文件可能为 0 字节或不存在" 现象完全吻合。

### G. **`subprocess.Popen + start_new_session=True` + `os.killpg` 与 FC 容器内 PID namespace 的细节** ⭐

- FC 自定义容器 PID 1 通常是 [start_server.sh](cci:7://file:///tmp/zfj/apps/facefusion-fc-gpu-acr/docker/start_server.sh:0:0-0:0)（继而是 `python api/server.py`），`os.setsid`/`start_new_session=True` 把 FaceFusion 移到一个新进程组没问题；
- 但 **FaceFusion 内部的 ffmpeg 子进程**也是 child；FaceFusion 本身退出后 ffmpeg 偶尔会成为孤儿但仍占用 GPU/磁盘。下一次 cold start 时若残留 ffmpeg 还在写 mp4，会让健康检查通过、却让 GPU 显存被吃掉，第二个 job 创建 ORT session 失败。
- 这个跟 0-byte / 间歇性失败的形态匹配。

### H. **`_track_facefusion_stage` 每 3 秒同步读取大 stdout/stderr 文件** ⭐

```python
facefusion_stdout_tail=_tail_file(job.get("facefusion_stdout_path"), 1200),
```

- `_log_level=debug` 下 FaceFusion 输出量很大，文件可能上百 MB；
- `_tail_file` 是 `read_bytes()[-max_chars:]`：**整文件读到内存再切尾**！
- 每 3 秒读一次，IO 抖动 + 内存峰值在 FC 受限实例上完全可能把 worker 拖到 GC pause，叠加 D/E 把单帧时间放大 2-3 倍。

### I. **FF_OPTIMIZE_TARGET_VIDEO 与 FaceFusion 的 fps/分辨率二次降级冲突** ⭐

- `_preprocess_target_video` 已经把目标视频降到 `360 px / 12 fps`；
- FaceFusion 又被传 `--output-video-fps 12`，但它**内部抽帧**默认按目标视频原始 fps；
- 如果预处理 ffmpeg 只把容器层 fps 改了 metadata 没真重编码（其实这里是 `-c:v libx264`，已重编码，OK），不会触发；但如果 `FF_OPTIMIZE_TARGET_VIDEO=0`，preview profile 用户上传 60fps 1080p 视频时 FaceFusion 会按 60×N 帧跑，**直接超时**。

## 3. 还有几个值得现场确认的细节

- **HTTP request timeout**：FC 自定义运行时的 HTTP 网关（`fcapp.run` 域名）默认 60s 单请求超时，对 `/api/swap/video/job` 同步 inline 模式致命；目前代码 `FF_VIDEO_JOB_MODE=background` 时只返回 job_id，没问题。但如果你环境变量被覆盖成 `inline`（Dockerfile 里就是写的 `inline`！），整个上传请求会在 FC 网关挂掉，App 端表现一样是"长时间无响应"。

  > ⚠️ Dockerfile L24：`FF_VIDEO_JOB_MODE=inline` 与代码默认值 `background` 冲突，且 [start_server.sh](cci:7://file:///tmp/zfj/apps/facefusion-fc-gpu-acr/docker/start_server.sh:0:0-0:0) 又有 `${FF_VIDEO_JOB_MODE:-inline}` 兜底——**这是最容易被忽视、又能完美复现"卡住"的配置错误**。建议先把它改回 `background`。

- **`OUTPUT_BASE = /tmp/facefusion-output`** vs **FaceFusion 内部 temp 在 FF_DIR/.assets/temp**：分散在两处目录，重启容器丢失结果但模型缓存也丢失（`.assets/models/` 在镜像层）。FC 实例每次冷启动的"第一次视频 job" 都吃 D+C 双重伤害。

- 现行 `FF_FACEFUSION_TIMEOUT_SECONDS` 在代码里默认 900s，但文档说的是 "600 秒超时"——确认一下部署环境实际 env，避免误以为提到的"600 秒"是代码的硬编码上限。

## 4. 排查优先级建议

按"投入产出比"排：

1. **先把 `FF_VIDEO_JOB_MODE` 在 Dockerfile/start_server.sh/部署 env 三处统一改成 `background`**（如已是 background 则跳过）。
2. **进容器看 `<FF_DIR>/.assets/models/` 模型是否齐全**，把 `face_detector_model` / `face_landmarker_model` / `face_recognizer_model` 用到的 onnx 烧进镜像，停止运行时下载（解决 C）。
3. **在 worker 里实测一次 ORT CUDA session 创建**，并在 `/api/health` 输出真实 `session.get_providers()` 而非 `ort.get_available_providers()`（解决 D）。
4. **取消 `OMP/OPENBLAS/MKL_NUM_THREADS=1`**，给 CPU 预/后处理 2-4 线程；保持 `FF_EXECUTION_THREAD_COUNT=1` 控制 GPU 并发即可（缓解 E）。
5. **修正进度上限**：把合成进度 0.85 改为读取 FaceFusion stdout（`PROGRESS:` 之类的关键字），让"卡 85%" 这个误报消失，同时 0-byte 检测改成"读文件能解析出 `moov`/`isom` ftyp"（解决 F）。
6. **把 `JOBS` 持久化到磁盘或外部 KV**（FC 同地域 Redis/OSS），让多实例和实例冻结后仍能查询 job（彻底解决 A）。

如果你愿意，我可以基于上述分析，逐项落地 **优先级 1-3** 的代码改动并配上 smoke 验证脚本。